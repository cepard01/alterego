import { describe, expect, it } from 'vitest';

// Stub app — placeholder until the dashboard UI exists. Keeps the
// workspace-wide `pnpm test` green.
describe('admin-dashboard', () => {
  it('is a stub', () => {
    expect(true).toBe(true);
  });
});
